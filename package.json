{
  "name": "clickprintbot",
  "scripts": {
    "dev": "nodemon --exec \"docker compose build --progress quiet && docker compose up -d && docker compose logs -f --tail 0 bot\"",
    "test": "node --test"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}