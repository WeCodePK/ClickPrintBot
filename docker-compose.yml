services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  bot:
    build: .
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      CLICKPRINT_API_URL: ${CLICKPRINT_API_URL:-https://clickprintbackend.wckd.pk}
      REDIS_URL: redis://redis:6379
      WWEBJS_DATA_PATH: /data
      WWEBJS_CLIENT_ID: ${WWEBJS_CLIENT_ID:-clickprint}
      TOKEN_TTL_SECONDS: ${TOKEN_TTL_SECONDS:-2592000}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # HTTP gateway (NotifyBot `/send` API): set API_KEY to enable it.
      # The container always listens on 3000 internally; publish it on the host
      # via GATEWAY_PORT below.
      API_KEY: ${API_KEY:-}
    ports:
      # Expose the HTTP gateway (`/send`, `/health`) on the host.
      - "${GATEWAY_PORT:-3000}:3000"
    volumes:
      # Persist the WhatsApp session so you only scan the QR once.
      - wweb-auth:/data
    # Keep a TTY so the QR code renders correctly in `docker compose logs`.
    stdin_open: true
    tty: true

volumes:
  redis-data:
  wweb-auth:
